<h1>Listing email_contents</h1>

<table>
  <tr>
    <th>Subject</th>
    <th>From</th>
    <th>Bcc</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @email_contents.each do |email_content| %>
  <tr>
    <td><%= email_content.subject %></td>
    <td><%= email_content.from %></td>
    <td><%= email_content.bcc %></td>
    <td><%= email_content.body %></td>
    <td><%= link_to 'Show', email_content %></td>
    <td><%= link_to 'Edit', edit_email_content_path(email_content) %></td>
    <td><%= link_to 'Destroy', email_content, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Email content', new_email_content_path %>
