<ul>
  <% if Admin::MeetingsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_meetings_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_meeting_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::MeetingsController.sortable? and Meeting.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Meetings"),
                 admin_meetings_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Meetings"),
                 admin_meetings_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>