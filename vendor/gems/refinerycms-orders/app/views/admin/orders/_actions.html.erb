<ul>
  <% if Admin::OrdersController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_orders_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_order_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::OrdersController.sortable? and Order.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Orders"),
                 admin_orders_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Orders"),
                 admin_orders_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>