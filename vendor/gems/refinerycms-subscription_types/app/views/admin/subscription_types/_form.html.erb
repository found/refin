<%= form_for [:admin, @subscription_type] do |f| -%>
  <%= render :partial => "/shared/admin/error_messages", :locals => {
          :object => @subscription_type,
          :include_object_name => true
        } %>
  
  <div class='field'>
    <%= f.label :title -%>
    <%= f.text_field :title, :class => 'larger widest' -%>
  </div>
  
  <div class='field'>
    <%= f.label :day -%>
    <%= f.text_field :day -%>
  </div>
  
  <%= render :partial => "/shared/admin/form_actions",
              :locals => {
                :f => f,
                :continue_editing => false,
                :delete_title => t('delete', :scope => 'admin.subscription_types.subscription_type'),
                :delete_confirmation => t('message', :scope => 'shared.admin.delete', :title => @subscription_type.title)
              } %>
<% end -%>
