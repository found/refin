<h3>Town Meetings</h3>
 <ul>
	<% Meeting.find(:all, :order => "created_at DESC", :limit => 3).each do |meeting| %>
		<li>
			<h4><%= link_to meeting.title, "" %></h4>

			<p><%= shorten(strip_tags(meeting.blurb), 180) %> <br /><br /><%= link_to "read more", "" %></p>
		</li>

	<% end %>
 </ul>