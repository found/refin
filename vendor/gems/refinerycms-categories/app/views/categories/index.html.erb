<% content_for :body_content_left do %>
  <ul id="categories">
    <% @categories.each do |category| %>
      <li>
        <%= link_to category.category_name, category_url(category) %><br />
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
