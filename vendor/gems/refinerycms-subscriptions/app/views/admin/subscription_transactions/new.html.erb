<h2>Create a new Receipt</h2>

<%= form_for @subscription_transaction, :url => admin_subscription_transactions_path do |f| %>
	<%= f.label :subscription_id %>
	<%= f.text_field :subscription_id %>
	<br />
	<%= f.label :amount %>
	<%= f.text_field :amount %>
<% end %>